import { QueryModal } from "components/common/modal/QueryModal";

export const EditCycleModal: React.FC = () => {
  return (
    <QueryModal size="full" urlParam="edit-cycle" title="Edit Cycle">
      <p>Foo</p>
    </QueryModal>
  );
};
